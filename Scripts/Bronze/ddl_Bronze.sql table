Create Schema Bronze;
Create Schema Silver;
Create Schema Gold;

CREATE TABLE bronze.crm_cust_info (
    cst_id              INT,
    cst_key             NVARCHAR(50),
    cst_firstname       NVARCHAR(50),
    cst_lastname        NVARCHAR(50),
    cst_marital_status  NVARCHAR(50),
    cst_gndr            NVARCHAR(50),
    cst_create_date     DATE
);

Create table bronze.crm_cust_info (
	Cst_id 			Int,
	cst_key 		Nvarchar(50),
	cst_firstname Nvarchar(50),
	cst_lastnamr
)

\copy bronze.crm_cust_info FROM '/Users/suraj/Downloads/sql-data-warehouse-project/datasets/source_crm/cust_info.csv' WITH (FORMAT CSV, HEADER TRUE, DELIMITER ',');--RUN IN TERMINAL 

select * From bronze.crm_cust_info

CREATE TABLE bronze.crm_prd_info (
    prd_id       INT,
    prd_key      VARCHAR(50),
    prd_nm       VARCHAR(50),
    prd_cost     INT,
    prd_line     VARCHAR(50),
    prd_start_dt DATE,
    prd_end_dt   DATE
);

SELECT * FROM bronze.crm_prd_info;

\copy bronze.crm_prd_info FROM'/Users/suraj/Downloads/sql-data-warehouse-project/datasets/source_crm/prd_info.csv' WITH (FORMAT CSV, HEADER TRUE, DELIMITER ',')--RUN IN TERMINAL 

CREATE TABLE bronze.crm_sales_details (
    sls_ord_num  VARCHAR(50),
    sls_prd_key  VARCHAR(50),
    sls_cust_id  INT,
    sls_order_dt INT,
    sls_ship_dt  INT,
    sls_due_dt   INT,
    sls_sales    INT,
    sls_quantity INT,
    sls_price    INT
);


SELECT * FROM bronze.crm_sales_details;

SELECT Count(*) FROM bronze.crm_sales_details;

\copy bronze.crm_sales_details FROM '/Users/suraj/Downloads/sql-data-warehouse-project/datasets/source_crm/sales_details.csv' WITH (FORMAT CSV, HEADER TRUE, DELIMITER ',')--RUN IN TERMINAL 


CREATE TABLE bronze.erp_loc_a101 (
    cid    VARCHAR(50),
    cntry  VARCHAR(50)
);

SELECT * FROM bronze.erp_loc_a101;

\copy bronze.erp_loc_a101 FROM '/Users/suraj/Downloads/sql-data-warehouse-project/datasets/source_erp/LOC_A101.csv' WITH (FORMAT CSV, HEADER TRUE, DELIMITER ',')--RUN IN TERMINAL

CREATE TABLE bronze.erp_cust_az12 (
    cid    VARCHAR(50),
    bdate  DATE,
    gen    VARCHAR(50)
);

SELECT * FROM bronze.erp_cust_az12;

\copy bronze.erp_cust_az12 FROM '/Users/suraj/Downloads/sql-data-warehouse-project/datasets/source_erp/CUST_AZ12.csv' WITH (FORMAT CSV, HEADER TRUE, DELIMITER ',')--RUN IN TERMINAL


CREATE TABLE bronze.erp_px_cat_glv2 (
    id           VARCHAR(50),
    cat          VARCHAR(50),
    subcat       VARCHAR(50),
    maintenance  VARCHAR(50)
);

SELECT * FROM bronze.erp_px_cat_glv2;
\copy bronze.erp_px_cat_glv2 FROM '/Users/suraj/Downloads/sql-data-warehouse-project/datasets/source_erp/PX_CAT_G1V2.csv' WITH (FORMAT CSV, HEADER TRUE, DELIMITER ',')--RUN IN TERMINAL


